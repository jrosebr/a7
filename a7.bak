#lang racket

;Problem 1
(define last-non-zero
  (lambda (ls)
    (let/cc k
      (letrec
          ((last-non-zero
            (lambda (ls)
              (cond
                ;; fill in lines here
                ))))
        (last-non-zero ls)))))

;Problem 2
